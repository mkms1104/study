spring:

### 스프링 부트는 기본적으로 h2 인메모리 방식으로 로드하므로 아무 설정이 없어도 된다.
#  datasource:
#    url: jdbc:h2:mem:test # 인메모리 모드
#    username: sa
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # 로그로 출력한다.
    org.hibernate.type: trace